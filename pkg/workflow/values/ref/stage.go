package ref

/*
 Need to implement
*/

/*
import "regexp"

var (
	stageRegexpString = `^\${stages.([-_\w]+).outputs.([-_\w]+)}$`
	stageRegexp       = regexp.MustCompile(stageRegexpString)
)

const (
	stageTypeRefFormat = `${stages.<stage>.outputs.<key>}`
)
*/
